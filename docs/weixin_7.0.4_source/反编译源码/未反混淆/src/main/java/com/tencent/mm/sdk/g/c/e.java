package com.tencent.mm.sdk.g.c;

import android.os.HandlerThread;
import android.util.Printer;

public interface e {
    HandlerThread a(Runnable runnable, String str, Printer printer);

    HandlerThread anO(String str);
}
