package com.tencent.tencentmap.mapsdk.maps.a;

import java.util.List;

interface kj<T, S extends kt> extends kv {
    List<kj<T, S>> a(kg<? extends T, ? extends S> kgVar);

    void a(mf<? super kt, Boolean> mfVar, ly<? super kg<T, S>> lyVar);

    int c();
}
